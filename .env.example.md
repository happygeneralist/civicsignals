# Signal generation
# Default provider is rules. Use gemini to enhance signal summaries during generation.
SIGNALS_PROVIDER=rules

# Required only when SIGNALS_PROVIDER=gemini.
GEMINI_API_KEY=

# Optional local override. The scheduled production workflow is deliberately pinned to
# gemini-2.5-flash; change that production model only in a reviewed source change.
GEMINI_MODEL=gemini-2.5-flash
